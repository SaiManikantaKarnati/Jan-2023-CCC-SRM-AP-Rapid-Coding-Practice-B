I M06 - All Char Compression

#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    char str[1000];
    int i;
    scanf("%s",str);
    int len = strlen(str);
    for(i=0;i<len;i++)
        if(str[i] != str[i+1])
            printf("%c",str[i]);
    
    return 0;
}
